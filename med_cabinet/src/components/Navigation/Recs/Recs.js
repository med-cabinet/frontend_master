import React from 'react';

const Recs = (props) => {

    // console.log(props.userData)

    return (
        <div>
            <p>{props.strainData.name}</p>
        </div>
    );
};

export default Recs;